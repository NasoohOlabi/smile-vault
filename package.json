{
  "name": "smile-vault",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "catalog": "bun s.js",
    "firebase:login": "bunx firebase-tools@latest login",
    "deploy": "bun run build && bunx firebase-tools@latest deploy --only hosting"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "tailwindcss": "^4.1.11",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  }
}
